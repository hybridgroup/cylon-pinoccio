'use strict';

var PinoccioPower = source("pinoccio-power");

describe("Cylon.Drivers.PinoccioPower", function() {
  var driver = new PinoccioPower({
    device: { connection: 'connect' }
  });

  it("needs tests");
});
